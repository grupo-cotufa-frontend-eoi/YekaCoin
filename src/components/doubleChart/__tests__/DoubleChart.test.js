
import DoubleChart from "../DoubleChart.vue";
import { render, screen } from "@testing-library/vue";
import "@testing-library/jest-dom";

describe("DoubleChart component tests", () => {
  it("The first selector should be rendered on the document", () => {
    /*
     * Render(DoubleChart);
     * expect(screen.getByTestId("first-selector")).toBeInTheDocument();
     */
  });
  it("The second selector should be rendered on the document", () => {
    /*
     * Render(DoubleChart);
     * expect(screen.getByTestId("second-selector")).toBeInTheDocument();
     */
  });
});
